
<% if session[:logged_in] == true %>
  <%= fb_connect_async_js%>

  <% #fb_logout_link("Logout #{current_facebook_user.last_name} #{current_facebook_user.first_name}", site_url()) %>
  <%= fb_logout_link("Logout", site_url()) %>
  <br>
  <%= "Welcome #{current_facebook_user.first_name} #{current_facebook_user.last_name} (#{current_facebook_user.email})" %>

    <%#= "id= #{current_facebook_user.birthday} alias = #{current_facebook_user.id} )" %>
    <%# fb_profile_pic(current_facebook_user.id, {:size => 'square' }) %>
    <%= image_tag "http://graph.facebook.com/#{current_facebook_user.id}/picture" %>

<% end %>








